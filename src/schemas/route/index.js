'use strict';

const {routeId} = require(`./route-id.schema`);
const {queryLimit} = require(`./query-limit.schema`);
const {queryOrder} = require(`./query-order.schema`);

module.exports = {
  routeId,
  queryLimit,
  queryOrder,
};
