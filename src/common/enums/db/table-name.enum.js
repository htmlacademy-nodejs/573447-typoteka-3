'use strict';

const TableName = {
  CATEGORIES: `categories`,
  COMMENTS: `comments`,
  USERS: `users`,
  ARTICLES: `articles`,
  ARTICLES_CATEGORIES: `articles_categories`,
};

module.exports = {
  TableName,
};
