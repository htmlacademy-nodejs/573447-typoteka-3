'use strict';

const {SsrPath} = require(`./ssr-path.enum`);
const {SsrMainPath} = require(`./ssr-main-path.enum`);
const {SsrArticlePath} = require(`./ssr-articles-path.enum`);
const {SsrMyPath} = require(`./ssr-my-path.enum`);

module.exports = {
  SsrPath,
  SsrMainPath,
  SsrArticlePath,
  SsrMyPath,
};
