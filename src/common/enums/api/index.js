'use strict';

const {HttpCode} = require(`./http-code.enum`);
const {ApiRoute} = require(`./api-route.enum`);
const {ApiMainRoute} = require(`./api-main-route.enum`);
const {ApiMyRoute} = require(`./api-my-route.enum`);
const {ApiArticlesRoute} = require(`./api-articles-route.enum`);

module.exports = {
  HttpCode,
  ApiRoute,
  ApiMainRoute,
  ApiMyRoute,
  ApiArticlesRoute,
};
