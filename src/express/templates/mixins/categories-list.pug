mixin categoriesList(categories = [], isArticleCategories = false)
  ul.themes(class=`${isArticleCategories ? `post__themes` : ``}`)
    each category in categories
      li.themes__item
        a.themes__item-link(href=`category/${category.id}`)
          | #{category.name}
          sup  1
