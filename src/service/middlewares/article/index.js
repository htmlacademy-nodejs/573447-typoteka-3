'use strict';

const {existArticle} = require(`./exist-article/exist-article`);
const {validateArticle} = require(`./validate-article/validate-article`);

module.exports = {
  existArticle,
  validateArticle,
};
