'use strict';

const {validateSchema} = require(`./validate-schema`);
const {validateParamSchema} = require(`./validate-param-schema`);

module.exports = {
  validateSchema,
  validateParamSchema,
};
