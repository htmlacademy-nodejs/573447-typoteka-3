'use strict';

const {calculatePagination} = require(`./calculate-pagination.helper`);
const {getTotalPagesCount} = require(`./get-total-pages-count.helper`);

module.exports = {
  calculatePagination,
  getTotalPagesCount,
};
